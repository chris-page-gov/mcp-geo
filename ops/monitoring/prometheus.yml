global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - /etc/prometheus/prometheus-alert-rules.yml

scrape_configs:
  - job_name: mcp-geo
    metrics_path: /metrics
    static_configs:
      - targets:
          - mcp-geo:8000
