# Deprecated local pointer: MCP Inspector

This placeholder is no longer maintained. Use the current MCP Inspector docs
from the canonical upstream sources rather than relying on `docs/vendor/openai/`.
