# Unattended Client Interop Reports

This subfolder contains unattended multi-client evaluation outputs and related
analysis for the host benchmarking harness.

New generated `client_interop_unattended_eval_*` artifacts in this folder are
ignored by git so local reruns do not clutter the top-level `docs/reports/`
directory or everyday `git status` output.
