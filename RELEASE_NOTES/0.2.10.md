# Release Notes 0.2.10 (2026-02-05)

## Added
- Spec package documentation plus Codex context guidance.
- OSM-backed static map render endpoint and data resources (boundary manifest/cache status/ONS cache).
- NOMIS toolset and stats routing for ONS vs NOMIS decisions.
- Claude debugging scripts and a local Claude Desktop wrapper for cached STDIO runs.
- MCP-Apps/OpenAI widget metadata and stdio profile defaults.

## Changed
- ONS query time-range expansion and OS Names WGS84 conversion support.
- Boundary validation and Docker ignore tuning for large data/logs.
- README/getting-started/PROGRESS updates for ONS cache and Claude local workflows.
- Devcontainer now persists Codex home across rebuilds.

## Fixed
- `admin_lookup.area_geometry` bbox inference when ArcGIS extents are missing.
- `os_names.nearest` accepts WGS84 lat/lon input.

## Tests
- Added coverage for map proxy, data resources, and circuit breaker behavior.
