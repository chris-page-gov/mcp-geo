# Release 0.2.5 - 2026-01-21

Focus: Streamable HTTP MCP transport and vendor snapshot packaging.

## Highlights
- Added `/mcp` Streamable HTTP JSON-RPC endpoint for ChatGPT/Inspector clients.
- Added HTTP trace proxy for inspecting MCP traffic.
- Added vendor snapshot tooling + packaging policy (release artifact, not git).

## Added
- Native `/mcp` Streamable HTTP JSON-RPC endpoint for MCP clients (ChatGPT, Inspector).
- HTTP trace proxy `scripts/mcp_http_trace_proxy.py` for capturing /mcp traffic.
- Vendor snapshot tooling (`scripts/vendor_fetch.sh`, `scripts/vendor_html_nojs.py`, `scripts/vendor_package.sh`).
- Vendor snapshot storage policy (`docs/vendor/README.md`).
- Placeholder OpenAI doc stash under `docs/vendor/openai/` for ChatGPT connector references.
- HTTP MCP tests covering initialize, tools/list, tools/call, and resources/read.
- Local OS map demo server `scripts/claude_serve_map.py`.

## Changed
- README/tutorial/ChatGPT setup updated for `/mcp` usage and HTTP trace proxy flow.
- Vendor docs now keep snapshots out of git and recommend release artifacts for HTML bundles.

## Fixed
- (none)

## Upgrade Notes
- Rebuild the Docker image after upgrading (`docker build -t mcp-geo-server .`).
- For offline docs, run `scripts/vendor_fetch.sh` then `scripts/vendor_package.sh` and attach the tarball to a release.

## Checksums & Integrity
Release artifact: `build/vendor-snapshot-20260121.tar.gz` (attach separately). Tag: `v0.2.5`.

---
Compare: `v0.2.4...v0.2.5`
