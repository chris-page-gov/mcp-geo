[pytest]
markers =
    integration: marks tests that perform real upstream HTTP calls (deselect with -m "not integration")
addopts = --maxfail=1 --disable-warnings --cov=server --cov=tools --cov-report=term-missing --cov-fail-under=90
testpaths = tests
norecursedirs = submodules docs/vendor
