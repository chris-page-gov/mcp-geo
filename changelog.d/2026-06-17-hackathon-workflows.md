Added

- Added native `os_workflows.descriptor` and `os_workflows.query` tools for the
  hackathon PoC workflow tranche: incident impact, batch address matching, and
  planning constraints.
- Added route-query coverage so affected-premises, batch UPRN matching, and
  planning-constraint prompts route to the new workflow surface.
- Added a hackathon PoC handoff report with users, data, MCP contract,
  governance controls, success measures, and four-week delivery scope.

Changed

- Updated the stakeholder live-run harness to exercise `os_workflows.query` for
  SG01, SG02, and SG05 instead of keeping the reusable orchestration solely in
  benchmark script code.
- Updated the MCP Geo skill guide with the new operational workflow entrypoints.
