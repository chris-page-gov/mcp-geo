### Added
- Added optional AI-client interop v2 coverage with OpenCode as a default matrix
  track, derived `naming_compat` / `core_capability` / `full` scenario packs,
  advisory pack recommendations for MCP-facing file changes, and report-to-report
  comparison modes for server or model/client deltas.

### Changed
- Updated the client interop runbook and evaluation docs to make full real-client
  runs optional rather than part of normal per-change validation.
