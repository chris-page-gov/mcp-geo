Added

- Added a deterministic OKF discovery pack generated from the MCP tool/resource registries and
  the checked-in OS API catalogue, with geospatial profiles, MCP bindings and integrity metadata.
- Exposed the six pack entrypoints as MCP resources and added a searchable list/map discovery UI
  without introducing a demo-only MCP tool contract.
- Added a meeting-ready OS discovery work package covering the Explorer House demonstration,
  OKF Explorer Map-tab follow-up, staged acceptance criteria and honest live-data boundaries.
- Added a deterministic, key-free GitHub Pages build with a public landing page, the discovery UI,
  all six OKF assets and a sendable OS discussion brief; live OS execution remains server-only.
